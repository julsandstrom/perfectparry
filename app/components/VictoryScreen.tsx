const VictoryScreen = () => {
  return (
    <div>
      <p className="text-4xl">You win!</p>

      <button>Restart</button>
      <button>Home</button>
    </div>
  );
};

export default VictoryScreen;
